using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UI : MonoBehaviour
{
    
    /*Renderer Images;

    void Start()
    {
        Images = GetComponentInChildren<Renderer>();
    }
    public static void Currentlives()
    {
        foreach (Renderer ImageEnCours in Images)
        {
            if (ImagesEnCours.name == "live1" && DataPlayer.Lives == 2)
            {
                ImagesEnCours.enabled = false;
            }
            if (ImagesEnCours.name == "live2" && DataPlayer.Lives == 1)
            {
                ImagesEnCours.enabled = false;
            }
            if (ImagesEnCours.name == "live1" && DataPlayer.Lives == 0)
            {
                ImagesEnCours.enabled = false;
            }
        }

    }
    public static void CurrentKeys()
    {
        if (DataPlayer.Keys == 1)
        {
            //MainCamera.Canvas.key2.display = false;
            //MainCamera.Canvas.key3.display = false;
        }
        else if (DataPlayer.Keys == 2)
        {
            //MainCamera.Canvas.key3.display = false;
        }
        else if (DataPlayer.Keys == 0)
        {
            //MainCamera.Canvas.key1.display = false;
            //MainCamera.Canvas.key2.display = false;
            //MainCamera.Canvas.key3.display = false;
        }
    }
    */


}
